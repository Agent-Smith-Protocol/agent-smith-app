import { ContentContainer } from '../layout/ContentContainer/ContentContainer.tsx';

export const HomePage = () => {
  return (
    <main>
      <ContentContainer>
        hello
      </ContentContainer>
    </main>
  )
}